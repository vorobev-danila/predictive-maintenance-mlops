apiVersion: v1
kind: Service
metadata:
  name: predictive-maintenance-api
  labels:
    app: predictive-maintenance-api
spec:
  type: NodePort
  selector:
    app: predictive-maintenance-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
